// Template for AppConfig.xcconfig.
// Render with scripts/render-appconfig.sh; do not edit the generated file by hand.
//
// `//` starts a comment in xcconfig, so URLs use the $() escape to break the
// sequence (e.g. `https:/$()/example.com/`). The render script does this for you.

RESUME_DATA_HOST = __RESUME_DATA_HOST__
RESUME_SHARE_URL = __RESUME_SHARE_URL__
RESUME_REPO_URL = __RESUME_REPO_URL__
